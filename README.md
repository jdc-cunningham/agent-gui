### Setup

#### Python virtual env

- `$python -m venv .venv`
- `source .venv/bin/activate`

(may need to run `sudo apt install python3.11-venv`)

I'm using Debian 11

- `$sudo apt install python3-tk`

